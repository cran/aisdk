% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_registry.R
\name{register_provider}
\alias{register_provider}
\title{Register a Provider Factory}
\usage{
register_provider(id, factory)
}
\arguments{
\item{id}{The provider ID (e.g. "deepseek").}

\item{factory}{A zero-argument function returning a provider object, or a
function of one argument (\code{model_id}) returning a language model.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
Register an additional provider factory so it can be resolved through the
default registry's \code{provider:model} syntax. Intended for companion packages
(such as \pkg{aisdk.providers}) that ship OpenAI-compatible providers and
register them from their \code{.onLoad} hook, e.g.
\code{aisdk::register_provider("deepseek", function() create_deepseek())}.

Registration is load-order independent: the factory is replayed into the
default registry whether it is registered before or after the registry is
first built.
}
