% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_introspect_tools.R
\name{r_eval_subprocess}
\alias{r_eval_subprocess}
\title{Run R code in an isolated subprocess with safety layers}
\usage{
r_eval_subprocess(
  code,
  timeout_secs = 30,
  working_dir = NULL,
  libpaths = NULL,
  envvars = NULL,
  share_credentials = FALSE
)
}
\description{
Uses \code{callr::r_bg()} plus a polling loop so the parent session can react to
\code{Ctrl-C} while a long-running or persistent subprocess (e.g. \code{system("vim")},
a hung network call) is executing. On timeout OR interrupt the subprocess
and its whole process tree are killed via \code{process$kill_tree()}.
}
\keyword{internal}
