% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic_adapter.R
\name{get_or_create_semantic_adapter_registry}
\alias{get_or_create_semantic_adapter_registry}
\title{Get or Create a Semantic Adapter Registry}
\usage{
get_or_create_semantic_adapter_registry(envir = NULL)
}
\arguments{
\item{envir}{Optional environment that stores \code{.semantic_adapter_registry}.
When \code{NULL}, a new default registry is created and returned without caching.}
}
\value{
A \code{SemanticAdapterRegistry} object.
}
\description{
Resolve the semantic adapter registry from an environment, or create and
cache a default registry when one is not already present.
}
\details{
This helper is safe for extension packages that need to share the active
semantic registry through a session or custom environment.
}
