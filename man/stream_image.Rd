% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_api.R
\name{stream_image}
\alias{stream_image}
\title{Stream Image Generation}
\usage{
stream_image(
  model,
  prompt,
  callback,
  output_dir = tempdir(),
  partial_images = 2,
  registry = NULL,
  ...
)
}
\arguments{
\item{model}{An \code{ImageModelV1} object or \code{provider:model} string.}

\item{prompt}{Prompt describing the desired image.}

\item{callback}{A function called for each partial/final event. The event
is a named list with \code{type} (\code{"partial"} or \code{"completed"}), \code{bytes} (raw
vector), \code{media_type} (e.g. \code{"image/png"}), \code{index} (integer for
partials), and \code{done} (logical).}

\item{output_dir}{Directory where the final image is written. Defaults to
\code{tempdir()}.}

\item{partial_images}{Integer 0–3 — how many preview frames to request.
Defaults to \code{2}. Set to \code{0} to disable previews and only receive the
final image via the callback.}

\item{registry}{Optional provider registry.}

\item{...}{Additional arguments passed to the model implementation
(e.g. \code{quality}, \code{output_format}, \code{background}, \code{size}).}
}
\value{
A \code{GenerateImageResult} with the final image.
}
\description{
Stream image generation with partial-image previews, useful for showing
the user progressive renders before the final image arrives. Currently
implemented for OpenAI's Responses API (\code{partial_images} 0–3); other
providers raise an informative error directing callers back to
\code{generate_image()}.
}
\examples{
\dontrun{
provider <- create_openai()
model <- provider$image_model("gpt-image-1.5")

stream_image(model, "a glowing nebula", callback = function(event) {
  if (event$type == "partial") {
    message(sprintf("Preview #\%d (\%d bytes)", event$index, length(event$bytes)))
  } else {
    message("Final image arrived: ", length(event$bytes), " bytes")
  }
})
}
}
