% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ask_ai.R
\name{ask_ai}
\alias{ask_ai}
\title{Ask aisdk About Recent R Context}
\usage{
ask_ai(
  prompt = NULL,
  model = NULL,
  skills = "auto",
  skill = NULL,
  context = NULL,
  startup_dir = getwd(),
  working_dir = tempdir(),
  sandbox_mode = "permissive",
  stream = TRUE,
  verbose = FALSE,
  show_context = FALSE,
  max_context_chars = Inf,
  max_error_age_secs = 300,
  confirm_stale_errors = TRUE,
  ...
)
}
\arguments{
\item{prompt}{Optional user instruction to add above the collected context.}

\item{model}{Optional model string, \code{LanguageModelV1}, or \code{ChatSession}.}

\item{skills}{Skill paths, \code{"auto"}, or a \code{SkillRegistry} passed to
\code{console_chat()}.}

\item{skill}{Optional skill name to force for the initial turn.}

\item{context}{Optional additional context text, or an \code{aisdk_ai_context}
object to reuse.}

\item{startup_dir}{R session startup directory for project-aware context.}

\item{working_dir}{Working directory for sandboxed console tools.}

\item{sandbox_mode}{Sandbox mode for the console agent.}

\item{stream}{Whether to stream model output.}

\item{verbose}{Whether to show debug console output.}

\item{show_context}{If \code{TRUE}, print and return the initial prompt without
launching \code{console_chat()}.}

\item{max_context_chars}{Maximum formatted context characters. Defaults to
\code{Inf}, meaning no explicit truncation.}

\item{max_error_age_secs}{Maximum age in seconds for errors/warnings to be
included. Defaults to 300 (5 minutes). Set to \code{Inf} to include all errors
regardless of age.}

\item{confirm_stale_errors}{If \code{TRUE} (default), show a warning and prompt
for confirmation when errors/warnings are detected but appear stale.}

\item{...}{Additional arguments passed to \code{collect_ai_context()}.}
}
\value{
Invisibly returns the \code{ChatSession} from \code{console_chat()}, or a
preview list when \code{show_context = TRUE}.
}
\description{
Collect recent R error context, active script context, session information,
and workspace object summaries, then open \code{console_chat()} with that context
as the initial prompt.

Collects the recent R error/session context and opens \code{console_chat()} with
that context as the first user message. In RStudio, this function also reads
the active source document when available, making it suitable as an Addin
binding.
}
