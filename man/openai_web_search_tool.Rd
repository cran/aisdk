% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openai_tools.R
\name{openai_web_search_tool}
\alias{openai_web_search_tool}
\title{OpenAI built-in web_search tool}
\usage{
openai_web_search_tool(
  allowed_domains = NULL,
  user_location = NULL,
  search_context_size = NULL,
  type = c("web_search", "web_search_preview")
)
}
\arguments{
\item{allowed_domains}{Optional character vector of domains the search is
restricted to. Forwarded as \code{filters.allowed_domains}.}

\item{user_location}{Optional named list with any of \code{city}, \code{country},
\code{region}, \code{timezone} for localized results.}

\item{search_context_size}{Optional \code{"low"}, \code{"medium"} (API default), or
\code{"high"} — controls how much retrieved context the model is given.}

\item{type}{The tool type identifier. Defaults to \code{"web_search"}; pass
\code{"web_search_preview"} for the older preview integration.}
}
\value{
A plain list ready to drop into \code{tools = list(...)}.
}
\description{
Build the configuration for OpenAI's model-hosted web search,
used via the Responses API. The model decides whether to search based on
the prompt.
}
\examples{
\dontrun{
model <- create_openai()$responses_model("gpt-5")
generate_text(
  model,
  "Latest material AAPL news?",
  tools = list(openai_web_search_tool(search_context_size = "high"))
)
}
}
