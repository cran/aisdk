% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openai_tools.R
\name{openai_hosted_mcp_tool}
\alias{openai_hosted_mcp_tool}
\title{OpenAI hosted MCP tool}
\usage{
openai_hosted_mcp_tool(
  server_label,
  server_url = NULL,
  connector_id = NULL,
  server_description = NULL,
  allowed_tools = NULL,
  require_approval = NULL,
  authorization = NULL,
  headers = NULL
)
}
\arguments{
\item{server_label}{Required label used to identify the server in tool
calls.}

\item{server_url}{URL of a custom MCP server. Exactly one of \code{server_url}
or \code{connector_id} is required.}

\item{connector_id}{OpenAI-managed connector ID (e.g. \code{"connector_gmail"}).}

\item{server_description}{Optional human-readable description.}

\item{allowed_tools}{Optional character vector of tool names to expose, or
a list filter (e.g. \code{list(read_only = TRUE)}).}

\item{require_approval}{\code{"always"}, \code{"never"}, or a list filter
(e.g. \code{list(always = list(tool_names = c("send_email")))}).}

\item{authorization}{Optional OAuth access token.}

\item{headers}{Optional named list of HTTP headers sent to the MCP server.}
}
\value{
A plain list ready to drop into \code{tools = list(...)}.
}
\description{
Give the model access to a remote MCP server, executed
server-side by OpenAI. This is independent of aisdk's local MCP client
(\code{R/mcp_client.R}) — that one connects from R, this one tells OpenAI to
connect on the model's behalf.
}
\examples{
\dontrun{
generate_text(
  model,
  "Roll 2d4+1",
  tools = list(openai_hosted_mcp_tool(
    server_label = "dmcp",
    server_url = "https://dmcp-server.deno.dev/sse",
    allowed_tools = "roll",
    require_approval = "never"
  ))
)
}
}
