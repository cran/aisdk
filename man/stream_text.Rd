% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_api.R
\name{stream_text}
\alias{stream_text}
\title{Stream Text}
\usage{
stream_text(
  model = NULL,
  prompt,
  callback = NULL,
  system = NULL,
  temperature = 0.7,
  max_tokens = NULL,
  tools = NULL,
  max_steps = 1,
  sandbox = FALSE,
  skills = NULL,
  session = NULL,
  hooks = NULL,
  registry = NULL,
  ...
)
}
\arguments{
\item{model}{Either a LanguageModelV1 object, or a string ID like "openai:gpt-4o".}

\item{prompt}{A character string prompt, or a list of messages.}

\item{callback}{A function called for each text chunk: \code{callback(text, done)}.}

\item{system}{Optional system prompt.}

\item{temperature}{Sampling temperature (0-2). Default 0.7.}

\item{max_tokens}{Maximum tokens to generate.}

\item{tools}{Optional list of Tool objects for function calling.}

\item{max_steps}{Maximum number of generation steps (tool execution loops).
Default 1. Set to higher values (e.g., 5) to enable automatic tool execution.}

\item{sandbox}{Logical. If TRUE, enables R-native programmatic sandbox mode.
See \code{generate_text} for details. Default FALSE.}

\item{skills}{Optional path to skills directory, or a SkillRegistry object.}

\item{session}{Optional ChatSession object for shared state.}

\item{hooks}{Optional HookHandler object.}

\item{registry}{Optional ProviderRegistry to use.}

\item{...}{Additional arguments passed to the model.}
}
\value{
A GenerateResult object (accumulated from the stream).
}
\description{
Generate text using a language model with streaming output.
This function provides a real-time stream of tokens through a callback.
}
\examples{
\donttest{
if (interactive()) {
  model <- create_openai()$language_model("gpt-4o")
  stream_text(model, "Tell me a story", callback = function(text, done) {
    if (!done) cat(text)
  })
}
}
}
