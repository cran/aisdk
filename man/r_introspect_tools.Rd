% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_introspect_tools.R
\name{r_introspect_tools}
\alias{r_introspect_tools}
\title{R Introspection Tools for Autonomous Debugging}
\description{
General-purpose primitives that let a console Agent enrich diagnostic
context on its own when R's \code{geterrmessage()} / \code{last.warning} /
\code{traceback()} snapshots are incomplete.

These tools are intentionally \strong{broad} (not problem-specific). Domain
tactics -- e.g. where to look for an install log, how to interpret an
Rcpp compilation error -- live in the \code{r-debug} skill (\verb{inst/skills/r-debug/}).
The tools provide eyes and hands; the skill provides the playbook.

Provided tools:
\itemize{
\item \code{r_eval}: run R code in an isolated subprocess, capture stdout,
stderr (including from grandchild processes like compilers and
\code{install.packages} subprocesses), messages, warnings, value, error.
\item \code{r_session_state}: structured snapshot of the live R session
(\code{.libPaths()}, repos, search path, key env vars, options, sessionInfo).
}
}
